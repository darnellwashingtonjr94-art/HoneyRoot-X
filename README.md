# HoneyRoot-X
HoneyRoot-X is a containerized SSH honeypot that baits attackers by simulating vulnerable root access. Built with Python and Paramiko, it logs brute-force credentials and intercepts post-exploitation commands within an isolated fake bash shell. Deployed via Docker, it captures actionable threat intelligence without ever risking the host system.
